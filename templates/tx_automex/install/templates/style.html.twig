<style>
    .g5i h1 {
        color: white;
        padding: 1rem;
        text-align: center;
        position: relative;
        margin: 1rem -0.5rem 0;
        line-height: 1.5;
        z-index: 1;
    }

    .g5i h1 .g5-title {
        text-transform: uppercase;
        font-size: 36px;  
        display: block;      
    }

    .g5i p {
        padding: 0 30%;
        font-weight: 500;
        margin: 0 0 20px;
        color: #ffffff;
        font-size: 1rem;
        text-shadow: none;
        text-align: center;
        border-radius: 3px;
        line-height: 1.5;
        position: relative;
        z-index: 1;
    }

    .g5i .g5-info {
        color: #ffffff;
        font-weight: 700;
        font-size: 18px;
    }

    .g5i .g5-actions {
        text-align: center;
        position: relative;
        padding: 1rem;
        z-index: 1;       
    }

    .g5i .g5-button {
        display: inline-block;
        font-size: 1rem;
        color: #130f40;
        border: 2px solid #fff;
        border-radius: 3px;
        padding: 12px 20px;
        background: #fff;
        vertical-align: middle;
        transition: background .2s;
        text-decoration: none;
        margin: 0 8px;
    }

    .g5i .g5-button:hover {
        background-color: rgba(255,255,255,0.9);
        transition: background .2s;
    }

    .g5i .g5-icon {
        line-height: 11px;
        vertical-align: middle;
    }

    .g5i .g5-rockettheme {
        background-image:url('../templates/tx_automex/images/logo-light.png');
        width: 130px;
        height: 31px;
        background-repeat: no-repeat;
        position: absolute;
        bottom: 5px;
        right: 5px;
        background-size: contain;
    }

    .g5i .g5-rockettheme a {
        display: block;
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        font-size: 0;
    }

    /* container */
    .g5i {
        position: relative;
        margin: 15px 15px 15px -15px;
        background: url('../templates/tx_automex/images/installation.jpg');
        padding: 1rem;
        background-repeat: no-repeat;
        background-size: cover;
        border-radius: 6px;
        position: relative;
        overflow: hidden;
    }
    .g5i::after {
        content: '';
        background-color: rgb(229, 62, 41, 0.7);
        position: absolute;
        width: 100%;
        height: 100%;
        top: 0;
        left: 0;
    }
</style>